<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración SOLO para desarrollo (build debug).

  Permite tráfico HTTP (cleartext) únicamente hacia los hosts locales
  usados mientras se trabaja contra FastAPI local:
    - 10.0.2.2        : Android Emulator -> host de la máquina
    - 127.0.0.1/localhost : dispositivo físico con `adb reverse tcp:8000 tcp:8000`

  No afecta a builds de release: este archivo vive únicamente en src/debug.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
